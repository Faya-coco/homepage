# Faya 个人作品集主页 — 需求规格说明书（SPEC 细化稿 v3 · Nost 手绘漫画杂志风格）

> 编写纪律：本 SPEC 严格遵循 R1–R6（见文末附录对照表）。核心五要素（问题陈述 / 方案描述 / 技术约束 / 明确不做 / 成功标准）均 **独立成章并显式标注**。
> 相较 v2，本章根据参考模板 **Nost**（BootstrapMB 创意手绘风格博客模板，预览地址：https://www.bootstrapmb.com/item/16625/preview）对视觉风格进行全面替换：从「糖果渐变梦幻」调整为「手绘漫画 / 杂志感 / 粗描边卡片」风格，配色、字体、装饰、布局、动效均同步更新。

---

## 1. 背景与问题陈述【五要素 · 问题陈述】

Faya 是一名开发者 / 工程师，希望以「个人品牌 / 影响力」为核心经营自己的线上门面。当前其专业能力、项目成果、联系方式散落在不同平台（GitHub、社交动态、聊天记录等），**没有一处统一、专业且有记忆点的入口**，导致同行、读者、潜在合作方无法在 10 秒内认知「Faya 是谁、做过什么、怎么找到他」。

本项目的目标是：用最低的长期维护成本，构建一个轻量、好更新、视觉有性格的单页作品集，把「关于我 / 项目 / 联系方式」沉淀到一个稳定可分享的网址上。

---

## 2. 目标

- **核心目标（已确认）**：建立个人品牌与行业影响力，而非直接求职或接单。
- **可衡量结果**：访客打开主页后，能在首屏获知 Faya 的身份与定位；向下滚动可在 3 屏内看到精选项目与联系入口。
- **非目标**：不用于投递简历、不用于直接成交订单（见第 7 章）。

---

## 3. 范围

**本期做（In Scope）**
- 单页（SPA）作品集，含 3 个内容板块：关于我 / 项目展示 / 简洁联系区。
- 移动端优先的响应式布局（手机 + 桌面）。
- 静态托管于 GitHub Pages，无后端、无数据库。
- 视觉风格按第 5.1 章「视觉设计规范」落地（Nost 手绘漫画杂志风格 / 珊瑚红主色 / 粗黑描边 / 米色背景 / 卡片式杂志布局 / 轻量趣味动效）。

**本期不做（Out of Scope）**：见第 7 章「明确不做 / Non-goals」。

---

## 4. 需求详情（每题均有结论，R2）

> 以下为需求澄清过程中提出的每一个问题，以及最终落点结论。结论已直接写死，不留悬空问题。

### 4.1 总体需求（第一轮访谈）

| 编号 | 澄清问题 | 结论（落点） |
|------|----------|--------------|
| Q1 | 主页最核心的目标是什么？ | **个人品牌 / 影响力**（非求职、非接单、非综合）。 |
| Q2 | Faya 从事哪个领域 / 职业？ | **开发者 / 工程师**。作品以项目、代码、技术内容为主。 |
| Q3 | 主页包含哪些内容板块？ | **关于我 / 简介** + **项目 / 作品展示**（不含博客、不含联系区当时）。 |
| Q4 | 希望给访客什么视觉第一印象？ | 方向为「活泼 / 有个性 / 手绘漫画感」，具体落见第 4.2 与第 5.1 章（已细化为 Nost 风格 6 项决策）。 |
| Q5 | 用什么技术方式实现？ | **前端框架**（非纯静态手写、非站点生成器）。 |
| Q6 | 具体用哪个框架？ | **Vue + Vite**（纯前端 SPA）。 |
| Q7 | 部署 / 托管在哪里？ | **GitHub Pages**（gh-pages，项目站点）。 |
| Q8 | 文字与项目内容现在怎么处理？ | **现在提供真实内容**：Faya 提供姓名、简介、项目、链接，直接写入数据文件。 |
| Q9 | 以下哪些本期明确不做？ | **不做暗色切换、不做多语言(i18n)、不做博客系统**。 |
| Q10 | 要不要放联系方式 / 社交链接区？ | **要，放一个简洁联系区**（GitHub / 邮箱 / 社交链接）。→ 故 Q3 追加「简洁联系区」。 |
| Q11 | 怎样算「做好了」（验收）？ | 移动端 + 桌面可用、易更新内容、有视觉记忆点（已转化为第 8 章可检验标准）。 |

**内容板块最终确认（合并 Q3 + Q10）**：关于我 / 简介、项目 / 作品展示、简洁联系区。

### 4.2 视觉风格细化（第二轮访谈，把「活泼 / 有个性」具体化为 Nost 风格）

| 编号 | 澄清问题 | 结论（落点） |
|------|----------|--------------|
| Q12 | 配色具体方向？ | **Nost 手绘漫画配色**：珊瑚红/番茄红（#FF6B6B）为主色，米白/浅灰（#F5F5F0）为背景，深炭灰（#2D2D2D）为文字与描边，柠檬黄（#FFE66D）为点缀。 |
| Q13 | 标题 / 正文字体气质？ | **漫画粗体 + 清晰无衬线**：标题用粗黑描边展示体（英文 `Shrikhand` / `Bangers`，中文用系统粗黑体 + 文字描边模拟），正文用清晰无衬线（英文 `Nunito` / `Inter`，中文 `思源黑体 / Noto Sans SC`）。 |
| Q14 | 形状与装饰元素？ | **手绘漫画描边与拼贴元素**：粗黑描边边框、偏移阴影（offset shadow）、虚线/波浪分隔线、圆角便签卡片、小星星/箭头/对话框装饰。 |
| Q15 | 动效强度？ | **轻量趣味动效**：卡片 hover 上浮 + 阴影偏移、标题描边颜色微变、小装饰元素缓慢摆动；**不采用 Canvas 粒子或鼠标跟随炫光**。 |
| Q16 | 整体布局气质？ | **杂志式卡片布局**：首屏大标题 + 头像/自我介绍卡片 + 项目卡片网格 + 联系区便签；大留白、模块化、边界清晰。 |
| Q17 | 标志性记忆符号？ | **专属漫画风格 IP**：一个贯穿全站的吉祥物 / 形象（如戴耳机的代码小怪兽、举着咖啡杯的小机器人），SVG 矢量，用于 Hero 主视觉、页脚、空状态。 |

---

## 5. 方案描述【五要素 · 方案描述】

构建一个基于 **Vue 3 + Vite** 的单页应用（SPA），构建产物为纯静态文件，部署到 **GitHub Pages**。页面采用「滚动式单页」信息架构，自上而下六个区块，各区块的视觉与交互均按第 5.1 章规范实现：

1. **导航栏（Nav）**：吸顶；品牌名用漫画粗体 + 小描边；锚点（关于 / 项目 / 联系）做成圆角胶囊按钮，hover 时背景反色。
2. **Hero（首屏）**：`min-height:100vh`；米白背景 + 顶部大标题「FAYA」用珊瑚红填充 + 深灰描边；头像/吉祥物 SVG 置于右侧；下方一张「自我介绍」便签卡片，带虚线边框和偏移阴影；双 CTA 按钮为粗描边胶囊。
3. **About（关于我）**：杂志分栏布局，左侧大标题「关于我」带波浪下划线，右侧为简介文字 + 技能栈 chip（粗描边小胶囊，hover 微转）。
4. **Projects（项目展示）**：卡片网格（桌面 3 列 / 平板 2 列 / 手机 1 列）；每张卡片为圆角矩形 + 粗黑描边 + 右下角偏移阴影；卡片顶部为项目 emoji/小图，标题粗体，技术栈标签为浅底色小胶囊。
5. **Contact（简洁联系区）**：一个大的「联系我」便签卡片，手写风格按钮 + IP 形象点缀 + GitHub / 邮箱链接做成圆角标签。
6. **Footer**：IP 形象小图标 + 漫画风格署名 + 年份。

**内容与逻辑分离**：所有可变内容集中于 `src/data/profile.js` 与 `src/data/projects.js` 两个数据文件，组件通过 `v-for` 渲染。新增项目 = 在数据数组追加一条对象，无需改动任何 `.vue` 组件。

### 5.1 视觉设计规范（Visual Design Spec，Nost 手绘漫画杂志风格）

#### 5.1.1 配色（Nost 风格，写死 hex）
- **主色（标题 / 重点按钮 / 高亮）**：`#FF6B6B`（珊瑚红 / 番茄红）。
- **背景色**：主背景 `#F5F5F0`（米白/浅灰），卡片背景 `#FFFFFF`，交替区块背景 `#EDECE6`（浅米灰）。
- **文字与描边**：`#2D2D2D`（深炭灰）正文与标题描边，`#6B6B6B` 次要文字。
- **点缀色**：`#FFE66D`（柠檬黄）用于小标签、高亮、星星；`#4ECDC4`（薄荷青）用于次要按钮/链接 hover。
- **约束**：主视觉必须出现「珊瑚红 + 深灰描边 + 米白背景」组合，保证品牌一致性；所有彩色元素均带深色描边或偏移阴影。

#### 5.1.2 字体（漫画粗体 + 清晰无衬线，写死字体族）
- **标题（英文）**：粗黑展示体 —— `Shrikhand` 或 `Bangers`（Google Fonts 引入），fallback 为 `Impact, sans-serif`。
- **标题（中文）**：系统粗黑体 —— `PingFang SC`, `Microsoft YaHei`, `Noto Sans SC`，配合 `text-shadow` 或 `-webkit-text-stroke` 实现描边效果。
- **正文（中 / 英）**：清晰圆体无衬线 —— 英文 `Nunito`，中文 `思源黑体 / Noto Sans SC`，保证可读性。
- **字号**：Hero 姓名桌面 `clamp(4rem, 12vw, 10rem)`，标题桌面 ≥ `3rem`（48px），正文 `1rem–1.125rem`。

#### 5.1.3 装饰（手绘漫画元素）
- **粗黑描边边框**：所有卡片、按钮、图片容器均带 `3px solid #2D2D2D` 描边。
- **偏移阴影**：卡片/按钮右下角带 `6px 6px 0 #2D2D2D` 或 `#FF6B6B` 的硬阴影，hover 时阴影位移或变色。
- **虚线/波浪分隔线**：板块之间用 `border-top: 3px dashed #2D2D2D` 或 SVG 波浪线分隔。
- **小装饰**：手绘星星、箭头、对话框、胶带贴纸；首屏与各区合计 ≥ 4 处。
- **便签卡片**：About 简介和 Contact 区使用带旋转角度（±2deg）的圆角便签卡片，增强拼贴感。

#### 5.1.4 动效（轻量趣味，写死方案）
- **卡片 hover**：`transform: translate(-4px, -4px)` + 阴影反方向位移，营造被按下的漫画感。
- **标题描边微动**：Hero 大标题在加载时描边颜色从 `#2D2D2D` 过渡到 `#FF6B6B` 再恢复，持续约 1.2s。
- **小装饰摆动**：星星/箭头装饰使用 CSS `@keyframes swing` 缓慢摆动（周期 3–5s，幅度 ±5deg）。
- **滚动揭示**：各板块进入视口时从下方滑入 24px + opacity 0→1，使用 Intersection Observer 触发。
- **尊重 `prefers-reduced-motion`**：自动停用 swing、滑入等动效，降级为静态布局。
- **约束**：不引入 Canvas 粒子、不引入鼠标跟随炫光、不引入第三方动画库。

#### 5.1.5 布局（杂志式卡片布局）
- **Hero 首屏**：`min-height: 100vh`；顶部居中/左对齐大标题；中间分为左右两栏（左侧自我介绍便签，右侧 IP 形象/头像）；底部 CTA 按钮组。
- **后续板块**：大留白 + 模块化卡片；标题居左或居中，内容以卡片网格呈现。
- **卡片网格断点**：桌面 3 列（≥1024px）、平板 2 列（≤768px）、手机 1 列（≤480px）。
- **容器宽度**：最大内容宽度 `1200px`，两侧留白 `16px（手机）/ 24px（平板）/ 48px（桌面）`。

#### 5.1.6 记忆符号（专属 IP 形象）
- 设计一个专属漫画风格 IP（建议：戴耳机的「代码小怪兽」或举着咖啡杯的「小机器人」），**SVG 矢量**，用于 Hero 主视觉、favicon、页脚、空状态。
- 约束：全站出现 ≥ 2 处同一 IP 形象（Hero + 页脚 / favicon）。
- 形象由 Faya 提供设定，或先出一版占位 SVG（`src/assets/mascot.svg`）待替换。

---

## 6. 技术约束【五要素 · 技术约束，R6】

| 项目 | 约束（写死，非「待定」） |
|------|--------------------------|
| 前端框架 | **Vue 3（^3.4）**，`<script setup>` 语法 |
| 构建工具 | **Vite（^5.2）** + **@vitejs/plugin-vue（^5.0）** |
| 语言 | 原生 ES Module（`"type": "module"`），无 TypeScript 编译负担（如需可后续加） |
| 样式方案 | 原生 CSS + CSS 变量（`scoped` 样式），**不引入重型 UI 库**（如 Element / Ant Design） |
| 字体 | 标题英文：`Shrikhand` / `Bangers`；正文：`Nunito` + `思源黑体 / Noto Sans SC`；经 Google Fonts `<link>` 引入 |
| 动效实现 | **纯 CSS 动画** + 原生 JS Intersection Observer；**不引入三方粒子 / 动画库**；尊重 `prefers-reduced-motion` |
| 部署平台 | **GitHub Pages**，通过 **GitHub Actions** 自动发布：`actions/upload-pages-artifact@v3` + `actions/deploy-pages@v4`，分支 `main` 推送即部署 |
| 构建基路径 | `vite.config.js` 中 `base: './'`（相对路径，兼容项目站点子路径与本地打开） |
| 适配范围 | 移动端优先；明确断点 **375px（手机）/ 768px（平板）/ 1280px（桌面）**，三档下均须完整可用 |
| 运行时依赖 | 仅 `vue`；无后端、无数据库、无第三方分析 SDK |

---

## 7. 明确不做 / Non-goals【五要素 · 明确不做，R4】

本版 **明确不做** 以下事项：

- ❌ **不做暗色 / 明暗切换主题**：本期只出「Nost 手绘漫画亮色」一套主题。
- ❌ **不做多语言（i18n）**：本期仅中文（`lang="zh-CN"`），不做语言切换。
- ❌ **不做博客 / 文章发布系统 / CMS**：主页只做静态展示，不维护文章发布与内容管理后台。
- ❌ **不接后端 / 不做表单提交 / 不做用户数据收集**：无服务端逻辑、无联系表单、无埋点统计。
- ❌ **不引入重型组件库或冗余运行时依赖**：保持轻量，避免依赖膨胀影响加载与维护。
- ❌ **不做 WebGL / Canvas 重特效**：动效仅限 CSS 动画与简单 JS 滚动触发，不引入 Canvas、WebGL、Three.js。
- ❌ **不引入三方粒子 / 动画库**（如 tsParticles / GSAP）：所有动效自写，控制体积与可控性。
- ❌ **不做搜索引擎 SEO 深度优化工程**：仅保证基础可索引（语义化标签、lang、标题），不投入结构化数据 / sitemap 等进阶 SEO。

---

## 8. 成功标准 / 验收【五要素 · 成功标准，R5】

> 每条标准均可用「是 / 否」或具体数值验证，杜绝「好看」「流畅」等主观词。

- **SC-1 响应式无溢出**：在视口宽度 **375px** 与 **1280px** 下，`document.documentElement.scrollWidth ≤ 视口宽度 + 1px`（即无横向滚动条），且各板块文字、卡片不重叠、不溢出。
  - *验证*：浏览器开发者工具切换设备尺寸目测 + 控制台读取 `scrollWidth`。判定：**是 / 否**。
- **SC-2 首屏性能**：`vite build` 后，桌面网络（Fast 3G 注明）下首屏可交互时间（TTI）**< 3s**；构建产物 gzip 后 JS+CSS 总体积 **< 200KB**。
  - *验证*：Lighthouse / WebPageTest 实测。判定：**数值对比**。
- **SC-3 内容可维护性**：新增 1 个项目只需在 `src/data/projects.js` 的 `projects` 数组追加 1 个对象（含 `name/emoji/desc/stack/links`），保存并 `npm run build` 成功后页面即出现该卡片，**无需修改任何 `.vue` 文件**。
  - *验证*：实操一次新增并构建成功。判定：**是 / 否**。
- **SC-4 视觉与动效（可检验，细化）**：
  - **SC-4a 配色**：主视觉出现「珊瑚红 #FF6B6B + 深灰描边 #2D2D2D + 米白背景 #F5F5F0」组合（查 CSS）。判定：**是 / 否**。
  - **SC-4b 字体**：标题使用 `Shrikhand` / `Bangers` 或带描边的粗黑体，正文为清晰无衬线。判定：**是 / 否**。
  - **SC-4c 装饰**：首屏与各区合计 **≥ 4 处**手绘漫画元素（粗描边卡片、偏移阴影、虚线分隔、星星/箭头）。判定：**是 / 否**。
  - **SC-4d 动效**：卡片 hover 有位移/阴影变化；滚动揭示使用 Intersection Observer；`prefers-reduced-motion` 下动效停止。判定：**是 / 否**。
  - **SC-4e 布局**：Hero `min-height ≥ 100vh`；桌面项目卡片网格为 3 列；标题桌面 `font-size ≥ 48px`。判定：**是 / 否**。
  - **SC-4f IP**：全站出现 **≥ 2 处**同一 IP 形象（Hero + 页脚 / favicon）。判定：**是 / 否**。
- **SC-5 部署可达**：GitHub Actions 工作流执行后，`https://<user>.github.io/<repo>/` 返回 HTTP **200**，且页面引用的 JS / CSS 资源无 **404**。
  - *验证*：`curl -I` 域名 + 浏览器网络面板。判定：**是 / 否**。
- **SC-6 基础可访问性**：所有可点击元素均为 `<a>` 或 `<button>` 且含可访问名称；根元素 `<html lang="zh-CN">`。
  - *验证*：DOM 检查。判定：**是 / 否**。

---

## 附录 A：待 Faya 提供的真实内容清单（执行阶段填入数据文件）

- 姓名 / 昵称、一句话定位、个人简介、技能栈列表
- 3–5 个项目：名称、emoji（可选）、描述、技术栈、GitHub / 演示链接
- 联系方式：GitHub、邮箱、Twitter / X、博客 链接
- **IP 形象设定**（二选一）：① Faya 提供形象描述 / 草图；② 允许我生成一版占位 SVG（`src/assets/mascot.svg`）待替换

> 说明：以上内容为用户提供的「数据 / 素材」，非设计决策，故不违反 R6（技术约束写死）。填入后即满足 SC-3 与 SC-4f。

---

## 附录 B：文件结构约定（实现阶段）

```
index.html
vite.config.js            # base: './'
package.json
src/
  main.js
  App.vue                 # 组合各板块 + 全局布局 + 滚动揭示指令
  components/
    Nav.vue               # 吸顶导航（漫画粗体品牌名 + 胶囊锚点）
    Hero.vue              # 100vh 首屏 + 大标题 + IP + 便签卡片 + CTA
    About.vue             # 杂志分栏 + 简介便签 + 技能 chip
    Projects.vue          # 粗描边卡片网格
    Contact.vue           # 联系便签卡片
    Footer.vue            # IP + 漫画署名
    Doodle.vue            # 手绘装饰 SVG（星星/箭头/波浪线）
  assets/
    mascot.svg            # 专属漫画风格 IP 形象（占位或终稿）
  data/
    profile.js            # 姓名/定位/简介/技能栈/联系方式
    projects.js           # 项目数组：name/emoji/desc/stack/links
  styles/
    fonts.css             # 字体 @import / Google Fonts 引入（SC-4b）
    global.css            # CSS 变量：Nost 配色、描边、阴影、布局
.github/workflows/deploy.yml   # GitHub Pages 自动部署
```

---

## 附录 C：编写规范对照（R1–R6）

| 规则 | 落实情况 |
|------|----------|
| **R1** 形式服务于表达，不照搬工单/后台结构，保留背景/目标/范围/需求详情/验收 | 按个人主页实际重组为 8 章，含背景、目标、范围、需求详情（含两轮访谈）、验收 |
| **R2** 每题必有落点 | 第 4 章 17 个问题（Q1–Q17）逐一给出结论，无悬空问题 |
| **R3** 五要素显性化 | 问题陈述 / 方案描述 / 技术约束 / 明确不做 / 成功标准 均独立成章并【五要素】标注 |
| **R4** Non-goals 单独成段 | 第 7 章独立列出「明确不做」清单（含动效边界） |
| **R5** 成功标准可检验 | 第 8 章 SC-1~SC-6（含 SC-4a~4f）均带具体数值或 是/否 判定，无主观词 |
| **R6** 技术约束写死 | 第 6 章框架/构建/部署/断点/字体/动效方案均写具体版本与方案，无「待定」 |

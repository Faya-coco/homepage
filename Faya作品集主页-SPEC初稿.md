# Faya 个人作品集主页 — 需求规格说明书（SPEC 细化稿 v2）

> 编写纪律：本 SPEC 严格遵循 R1–R6（见文末附录对照表）。核心五要素（问题陈述 / 方案描述 / 技术约束 / 明确不做 / 成功标准）均 **独立成章并显式标注**。
> 相较初稿，本轮将笼统的「活泼 / 有个性」经苏格拉底访谈确认为 **6 项具体设计决策**，并细化为第 5.1 章「视觉设计规范」，各板块规划与验收标准同步精确化。

---

## 1. 背景与问题陈述【五要素 · 问题陈述】

Faya 是一名开发者 / 工程师，希望以「个人品牌 / 影响力」为核心经营自己的线上门面。当前其专业能力、项目成果、联系方式散落在不同平台（GitHub、社交动态、聊天记录等），**没有一处统一、专业且有记忆点的入口**，导致同行、读者、潜在合作方无法在 10 秒内认知「Faya 是谁、做过什么、怎么找到他」。

本项目的目标是：用最低的长期维护成本，构建一个轻量、好更新、视觉有性格的单页作品集，把「关于我 / 项目 / 联系方式」沉淀到一个稳定可分享的网址上。

---

## 2. 目标

- **核心目标（已确认）**：建立个人品牌与行业影响力，而非直接求职或接单。
- **可衡量结果**：访客打开主页后，能在首屏获知 Faya 的身份与定位；向下滚动可在 3 屏内看到精选项目与联系入口。
- **非目标**：不用于投递简历、不用于直接成交订单（见第 7 章）。

---

## 3. 范围

**本期做（In Scope）**
- 单页（SPA）作品集，含 3 个内容板块：关于我 / 项目展示 / 简洁联系区。
- 移动端优先的响应式布局（手机 + 桌面）。
- 静态托管于 GitHub Pages，无后端、无数据库。
- 视觉风格按第 5.1 章「视觉设计规范」落地（糖果渐变 / 手写字体 / 手绘涂鸦 / Canvas 炫酷动效 / 大图冲击 / 专属 IP）。

**本期不做（Out of Scope）**：见第 7 章「明确不做 / Non-goals」。

---

## 4. 需求详情（每题均有结论，R2）

> 以下为需求澄清过程中提出的每一个问题，以及最终落点结论。结论已直接写死，不留悬空问题。

### 4.1 总体需求（第一轮访谈）

| 编号 | 澄清问题 | 结论（落点） |
|------|----------|--------------|
| Q1 | 主页最核心的目标是什么？ | **个人品牌 / 影响力**（非求职、非接单、非综合）。 |
| Q2 | Faya 从事哪个领域 / 职业？ | **开发者 / 工程师**。作品以项目、代码、技术内容为主。 |
| Q3 | 主页包含哪些内容板块？ | **关于我 / 简介** + **项目 / 作品展示**（不含博客、不含联系区当时）。 |
| Q4 | 希望给访客什么视觉第一印象？ | 方向为「活泼 / 有个性」，具体落见第 4.2 与第 5.1 章（已细化为 6 项决策）。 |
| Q5 | 用什么技术方式实现？ | **前端框架**（非纯静态手写、非站点生成器）。 |
| Q6 | 具体用哪个框架？ | **Vue + Vite**（纯前端 SPA）。 |
| Q7 | 部署 / 托管在哪里？ | **GitHub Pages**（gh-pages，项目站点）。 |
| Q8 | 文字与项目内容现在怎么处理？ | **现在提供真实内容**：Faya 提供姓名、简介、项目、链接，直接写入数据文件。 |
| Q9 | 以下哪些本期明确不做？ | **不做暗色切换、不做多语言(i18n)、不做博客系统**。 |
| Q10 | 要不要放联系方式 / 社交链接区？ | **要，放一个简洁联系区**（GitHub / 邮箱 / 社交链接）。→ 故 Q3 追加「简洁联系区」。 |
| Q11 | 怎样算「做好了」（验收）？ | 移动端 + 桌面可用、易更新内容、有视觉记忆点（已转化为第 8 章可检验标准）。 |

**内容板块最终确认（合并 Q3 + Q10）**：关于我 / 简介、项目 / 作品展示、简洁联系区。

### 4.2 视觉风格细化（第二轮访谈，把「活泼 / 有个性」具体化）

| 编号 | 澄清问题 | 结论（落点） |
|------|----------|--------------|
| Q12 | 配色具体方向？ | **糖果渐变**：粉→紫→青多色渐变流动，梦幻通透。 |
| Q13 | 标题 / 正文字体气质？ | **手写随性**：标题用手写体（Caveat / 马善政楷书），正文用清晰无衬线（Nunito / 思源黑体）。 |
| Q14 | 形状与装饰元素？ | **手绘涂鸦**：不规则手绘描边、波浪下划线高亮、箭头、星星。 |
| Q15 | 动效强度？ | **炫酷特效**：Canvas 粒子背景 + 鼠标跟随 / 烟花响应。 |
| Q16 | 整体布局气质？ | **大图冲击**：首屏全屏、超大字号、IP 形象大图。 |
| Q17 | 标志性记忆符号？ | **专属 IP 形象**：一个贯穿全站的吉祥物 / 形象。 |

---

## 5. 方案描述【五要素 · 方案描述】

构建一个基于 **Vue 3 + Vite** 的单页应用（SPA），构建产物为纯静态文件，部署到 **GitHub Pages**。页面采用「滚动式单页」信息架构，自上而下六个区块，各区块的视觉与交互均按第 5.1 章规范实现：

1. **导航栏（Nav）**：吸顶半透明；品牌名用手写体 + 涂鸦下划线；锚点（关于 / 项目 / 联系）点击平滑滚动。
2. **Hero（首屏）**：`height:100vh` 全屏；糖果渐变背景 + Canvas 粒子层 + 手写超大姓名（clamp 最高 8rem）+ IP 形象大图 + 双 CTA；光标跟随柔光。
3. **About（关于我）**：手写大标题 + 涂鸦波浪下划线高亮关键词 + 个人简介 + 技能栈 chip（hover 微转）。
4. **Projects（项目展示）**：大图冲击卡片网格（桌面 3 列 / 平板 2 列 / 手机 1 列）；卡片 hover 上浮 + 涂鸦描边边框；技术栈标签。
5. **Contact（简洁联系区）**：手写按钮 + IP 形象点缀 + GitHub / 邮箱 / Twitter / 博客 链接。
6. **Footer**：IP 形象 + 手写署名 + 年份。

**内容与逻辑分离**：所有可变内容集中于 `src/data/profile.js` 与 `src/data/projects.js` 两个数据文件，组件通过 `v-for` 渲染。新增项目 = 在数据数组追加一条对象，无需改动任何 `.vue` 组件。

### 5.1 视觉设计规范（Visual Design Spec，细化「活泼 / 有个性」）

#### 5.1.1 配色（糖果渐变，写死 hex）
- 渐变主色带（用于标题 / 色块 / 按钮）：`#FF7EB3`（粉）→ `#8A5CFF`（紫）→ `#2BD9C5`（青）→ `#FFD86B`（黄点缀）。
- 背景：主背景 `#FFF7FB`（极淡粉），区块交替背景 `#FDF2FF`。
- 文字：`#1F1430`（深紫黑）正文，`#6B5B7E` 次要。
- 约束：所有主渐变必须含「粉→紫→青」三段，保证品牌一致性（可数验证）。

#### 5.1.2 字体（手写随性，写死字体族）
- 标题（中 / 英）：手写展示体 —— 英文 `Caveat`，中文 `马善政楷书`（webfont 引入）。
- 正文（中 / 英）：清晰圆体无衬线 —— 英文 `Nunito`，中文 `思源黑体 / Noto Sans SC`，保证可读性。
- 字号：标题桌面 ≥ `4rem`（64px）；Hero 姓名 `clamp(3rem, 12vw, 8rem)`。

#### 5.1.3 装饰（手绘涂鸦）
- 用手绘风 SVG：波浪下划线高亮、箭头、星星、涂鸦边框；描边用 `filter: feTurbulence` 制造抖动笔触感。
- 约束：首屏与各区合计 ≥ 3 处涂鸦元素（下划线高亮关键词、箭头指引、角落星星）。

#### 5.1.4 动效（炫酷特效，写死方案）
- Hero 区加 `<canvas>` 粒子层：60–80 个渐变圆点缓慢漂浮，**鼠标移动时粒子产生吸引 / 排斥跟随响应**。
- 自写轻量 Canvas（vanilla JS，`requestAnimationFrame`），**不引入三方粒子库**；DPR 上限 2，离开视口 `visibilitychange` 暂停渲染。
- 光标跟随柔光（CSS 径向渐变层，`mousemove` 更新位置）。
- 尊重 `prefers-reduced-motion`：自动停用粒子与光标特效，降级为静态渐变。

#### 5.1.5 布局（大图冲击）
- Hero 首屏 `height:100vh`（±1px），姓名超大字号 + 全屏糖果渐变 + IP 形象大图（右侧 / 背景）。
- 后续板块大留白 + 大标题；卡片网格断点：桌面 3 列、平板（≤768px）2 列、手机（≤480px）1 列。

#### 5.1.6 记忆符号（专属 IP 形象）
- 设计一个专属 IP（建议：戴眼镜的「代码小星球」或「小机器人」），**SVG 矢量**，用于 Hero 主视觉、favicon、页脚、空状态。
- 约束：全站出现 ≥ 2 处同一 IP 形象（Hero + 页脚 / favicon）。
- 形象由 Faya 提供设定，或先出一版占位 SVG（`src/assets/mascot.svg`）待替换。

---

## 6. 技术约束【五要素 · 技术约束，R6】

| 项目 | 约束（写死，非「待定」） |
|------|--------------------------|
| 前端框架 | **Vue 3（^3.4）**，`<script setup>` 语法 |
| 构建工具 | **Vite（^5.2）** + **@vitejs/plugin-vue（^5.0）** |
| 语言 | 原生 ES Module（`"type": "module"`），无 TypeScript 编译负担（如需可后续加） |
| 样式方案 | 原生 CSS + CSS 变量（`scoped` 样式），**不引入重型 UI 库**（如 Element / Ant Design） |
| 字体 | 标题：`Caveat` + `马善政楷书`；正文：`Nunito` + `思源黑体 / Noto Sans SC`；经 Google Fonts / webfont `<link>` 引入（见第 5.1.2） |
| 动效实现 | Hero 粒子为**自写 2D Canvas**（vanilla JS），**不引入三方粒子 / 动画库**；性能约束：粒子数 **60–80**、DPR 上限 **2**、`requestAnimationFrame` 驱动、离开视口 `visibilitychange` 暂停、`prefers-reduced-motion` 降级 |
| 部署平台 | **GitHub Pages**，通过 **GitHub Actions** 自动发布：`actions/upload-pages-artifact@v3` + `actions/deploy-pages@v4`，分支 `main` 推送即部署 |
| 构建基路径 | `vite.config.js` 中 `base: './'`（相对路径，兼容项目站点子路径与本地打开） |
| 适配范围 | 移动端优先；明确断点 **375px（手机）/ 768px（平板）/ 1280px（桌面）**，三档下均须完整可用 |
| 运行时依赖 | 仅 `vue`；无后端、无数据库、无第三方分析 SDK |

---

## 7. 明确不做 / Non-goals【五要素 · 明确不做，R4】

本版 **明确不做** 以下事项（区分「产品思维」与「乱提需求」）：

- ❌ **不做暗色 / 明暗切换主题**：本期只出「糖果渐变亮色」一套主题。
- ❌ **不做多语言（i18n）**：本期仅中文（`lang="zh-CN"`），不做语言切换。
- ❌ **不做博客 / 文章发布系统 / CMS**：主页只做静态展示，不维护文章发布与内容管理后台。
- ❌ **不接后端 / 不做表单提交 / 不做用户数据收集**：无服务端逻辑、无联系表单、无埋点统计。
- ❌ **不引入重型组件库或冗余运行时依赖**：保持轻量，避免依赖膨胀影响加载与维护。
- ❌ **不做 WebGL 重特效**：动效仅限 2D Canvas 粒子，不引入 WebGL / Three.js 等重运行时。
- ❌ **不引入三方粒子 / 动画库**（如 tsParticles / GSAP）：粒子与动效均自写，控制体积与可控性。
- ❌ **不做搜索引擎 SEO 深度优化工程**：仅保证基础可索引（语义化标签、lang、标题），不投入结构化数据 / sitemap 等进阶 SEO。

---

## 8. 成功标准 / 验收【五要素 · 成功标准，R5】

> 每条标准均可用「是 / 否」或具体数值验证，杜绝「好看」「流畅」等主观词。

- **SC-1 响应式无溢出**：在视口宽度 **375px** 与 **1280px** 下，`document.documentElement.scrollWidth ≤ 视口宽度 + 1px`（即无横向滚动条），且各板块文字、卡片不重叠、不溢出。
  - *验证*：浏览器开发者工具切换设备尺寸目测 + 控制台读取 `scrollWidth`。判定：**是 / 否**。
- **SC-2 首屏性能**：`vite build` 后，桌面网络（Fast 3G 注明）下首屏可交互时间（TTI）**< 3s**；构建产物 gzip 后 JS+CSS 总体积 **< 200KB**。
  - *验证*：Lighthouse / WebPageTest 实测。判定：**数值对比**。
- **SC-3 内容可维护性**：新增 1 个项目只需在 `src/data/projects.js` 的 `projects` 数组追加 1 个对象（含 `name/emoji/desc/stack/links`），保存并 `npm run build` 成功后页面即出现该卡片，**无需修改任何 `.vue` 文件**。
  - *验证*：实操一次新增并构建成功。判定：**是 / 否**。
- **SC-4 视觉与动效（可检验，细化）**：
  - **SC-4a 配色**：全站至少 1 处含「粉→紫→青」三段渐变（查 CSS `linear-gradient`）。判定：**是 / 否**。
  - **SC-4b 字体**：标题 `font-family` 含手写体（Caveat / 马善政），正文为清晰无衬线。判定：**是 / 否**。
  - **SC-4c 装饰**：首屏与各区合计 **≥ 3 处**手绘涂鸦 SVG 元素（下划线 / 箭头 / 星星）。判定：**是 / 否**。
  - **SC-4d 动效**：Hero 的 `<canvas>` 粒子在加载后持续运行；鼠标移动时粒子坐标发生偏移响应；`prefers-reduced-motion` 下该层不渲染。DevTools Performance 测得帧率 **≥ 30fps**。判定：**是 / 否 + 数值**。
  - **SC-4e 布局**：Hero 高度 = `100vh`（±1px），桌面下姓名标题 `font-size ≥ 64px`。判定：**是 / 否**。
  - **SC-4f IP**：全站出现 **≥ 2 处**同一 IP 形象（Hero + 页脚 / favicon）。判定：**是 / 否**。
- **SC-5 部署可达**：GitHub Actions 工作流执行后，`https://<user>.github.io/<repo>/` 返回 HTTP **200**，且页面引用的 JS / CSS 资源无 **404**。
  - *验证*：`curl -I` 域名 + 浏览器网络面板。判定：**是 / 否**。
- **SC-6 基础可访问性**：所有可点击元素均为 `<a>` 或 `<button>` 且含可访问名称；根元素 `<html lang="zh-CN">`。
  - *验证*：DOM 检查。判定：**是 / 否**。

---

## 附录 A：待 Faya 提供的真实内容清单（执行阶段填入数据文件）

- 姓名 / 昵称、一句话定位、个人简介、技能栈列表
- 3–5 个项目：名称、emoji（可选）、描述、技术栈、GitHub / 演示链接
- 联系方式：GitHub、邮箱、Twitter / X、博客 链接
- **IP 形象设定**（二选一）：① Faya 提供形象描述 / 草图；② 允许我生成一版占位 SVG（`src/assets/mascot.svg`）待替换

> 说明：以上内容为用户提供的「数据 / 素材」，非设计决策，故不违反 R6（技术约束写死）。填入后即满足 SC-3 与 SC-4f。

---

## 附录 B：文件结构约定（实现阶段）

```
index.html
vite.config.js            # base: './'
package.json
src/
  main.js
  App.vue                 # 组合各板块 + 全局布局 + 滚动揭示指令
  components/
    Nav.vue               # 吸顶导航（手写品牌名 + 涂鸦下划线）
    Hero.vue              # 100vh 首屏 + 姓名 + IP + CTA
    CanvasParticles.vue   # 自写 2D Canvas 粒子层（SC-4d）
    Doodle.vue            # 手绘涂鸦 SVG 装饰（SC-4c）
    About.vue             # 简介 + 技能 chip
    Projects.vue          # 大图冲击卡片网格
    Contact.vue           # 简洁联系区
    Footer.vue            # IP + 手写署名
  assets/
    mascot.svg            # 专属 IP 形象（占位或终稿）
  data/
    profile.js            # 姓名/定位/简介/技能栈/联系方式
    projects.js           # 项目数组：name/emoji/desc/stack/links
  styles/
    fonts.css             # 字体 @font-face / Google Fonts 引入（SC-4b）
    global.css            # CSS 变量：糖果渐变配色、布局、动效
.github/workflows/deploy.yml   # GitHub Pages 自动部署
```

---

## 附录 C：编写规范对照（R1–R6）

| 规则 | 落实情况 |
|------|----------|
| **R1** 形式服务于表达，不照搬工单/后台结构，保留背景/目标/范围/需求详情/验收 | 按个人主页实际重组为 8 章，含背景、目标、范围、需求详情（含两轮访谈）、验收 |
| **R2** 每题必有落点 | 第 4 章 17 个问题（Q1–Q17）逐一给出结论，无悬空问题 |
| **R3** 五要素显性化 | 问题陈述 / 方案描述 / 技术约束 / 明确不做 / 成功标准 均独立成章并【五要素】标注 |
| **R4** Non-goals 单独成段 | 第 7 章独立列出「明确不做」清单（含动效边界） |
| **R5** 成功标准可检验 | 第 8 章 SC-1~SC-6（含 SC-4a~4f）均带具体数值或 是/否 判定，无主观词 |
| **R6** 技术约束写死 | 第 6 章框架/构建/部署/断点/字体/动效方案均写具体版本与方案，无「待定」 |
